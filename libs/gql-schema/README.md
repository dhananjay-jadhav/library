# gql-schema

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build gql-schema` to build the library.

## Running unit tests

Run `nx test gql-schema` to execute the unit tests via [Jest](https://jestjs.io).
